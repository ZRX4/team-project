// Командный проект. Группа ПИ-52.
// Команда: Кривоносов Валерий (в. 33), Щербаков Алексей (в. 30,техлид).
#include <iostream>

// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
#include "krivonosov.h"
#include "scherbakov.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===

using namespace std;

int main() {
	int choice;

	do {
		cout << "\n=== Командный проект: сборник расчётов ===\n";
		cout << "1. Количество теплоты Q = c*m*dt\n";
		cout << "2. Изменение температуры dt = Q/(c*m)\n";
		cout << "0. Выход\n";
		cout << "Выберите пункт: ";

		cin >> choice;

		switch (choice) {
		case 1:
			cout << "Введите удельную теплоемкость (джоуль на кельвин) c: ";
			cin >> c;
			cout << "Введите массу (в килограммах) m: ";
			cin >> m;
			cout << "Введите изменение температуры (в градусах кельвина) dt: ";
			cin >> dt;
			cout << "Количество теплоты Q = " << heat(c, m, dt) << " Дж\n";
			break;
		case 2:
			cout << "Введите количество теплоты (в джоулях) Q: ";
			cin >> Q;
			cout << "Введите удельную теплоёмкость (джоуль на кельвин) c: ";
			cin >> c;
			cout << "Введите массу (в киллограммах) m: ";
			cin >> m;

			if (c == 0 || m == 0) {
				cout << "Ошибка: c и m не должны быть равны нулю.\n";
			}

			else {
				cout << "Изменение температуры dt = "
					<< tempChange(Q, c, m) << "\n";
			}
			break;
case 0:
cout << "Работа завершена.\n";
break;
default:
cout << "Такого пункта нет.\n";
}
} while (choice != 0);
return 0;
}
